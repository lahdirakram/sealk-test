{
    "name": "@sealk/open",
    "version": "0.0.1",
    "scripts": {
        "start": "node main.js",
        "clean": "rm -rf ./dist",
        "compile": "tsc -p tsconfig.json",
        "compile-watch": "tsc -p tsconfig.json -w"
    },
    "dependencies": {
        "tsc": "^2.0.3",
        "typescript": "^4.3.3"
    },
    "devDependencies": {
        "@types/node": "^15.12.2"
    }
}
